services:
  meilisearch:
    image: getmeili/meilisearch:v1.39
    container_name: meilisearch
    restart: unless-stopped
    ports:
      - "7700:7700"
    env_file:
      - .env
    volumes:
      - meilisearch_data:/meili_data

volumes:
  meilisearch_data:
